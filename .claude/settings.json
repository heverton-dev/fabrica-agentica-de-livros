{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cat >/dev/null || true; command -v code-review-graph >/dev/null 2>&1 || exit 0; git rev-parse --git-dir >/dev/null 2>&1 && code-review-graph update --skip-flows --repo \"$(git rev-parse --show-toplevel 2>/dev/null)\" || true",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "cat >/dev/null || true; git rev-parse --git-dir >/dev/null 2>&1 || exit 0; changed=$(git diff --name-only 2>/dev/null); echo \"$changed\" | grep -qE 'docs/(manual-completo-fabrica|guia-execucao-maquina-vendas)\\.md|templates/template\\.typ' && python scripts/atualizar-documentacao.py --se-sujo --silencioso || true",
            "timeout": 120
          },
          {
            "type": "command",
            "matcher": "output/*/livros/*/capitulos/cap_*.md",
            "command": "bash scripts/validar_capitulo.sh $FILE 2>/dev/null && echo '[GATES OK]' || echo '[GATES FALHOU - Revisar capítulo]'",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cat >/dev/null || true; command -v code-review-graph >/dev/null 2>&1 || exit 0; git rev-parse --git-dir >/dev/null 2>&1 && code-review-graph status --repo \"$(git rev-parse --show-toplevel 2>/dev/null)\" || echo 'Not a git repo, skipping'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
